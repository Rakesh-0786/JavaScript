var a=1;
var b="1";
// typeof(a);
// typeof(b);
// if(a===b)
// With three equal sign(===),its also checking to see that the data types are matching or not if the data types of both variable are matching then it print the true value and if the data types are not matching then it print the false value.
// whereas with the two equal sign it doesnot care,means in two equal sign(==) only the the both variable sign are matching and  not compulsory to mathcing data type .
if(a==b){   
    console.log("yes");

}else{
    console.log("no");
}